#include <stdio.h>

int main() {
    
    int i;
    int result = 2;  

    for (i = 0; result <= 512; i++) {
        printf("2^%d = %d\n", i, result);
        result = result * 2;  
        printf("runs of to 2-512: ");
    }

    return 0;
}
